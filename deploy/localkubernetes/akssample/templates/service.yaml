apiVersion: v1
kind: Service
metadata:
  name: {{ include "akssample.fullname" . }}
  labels:
    {{- include "akssample.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "akssample.selectorLabels" . | nindent 4 }}
